import { documentType } from "@label/core";

async function updateComputedAdditionalTermsToAnnotate(
  _id: documentType['_id'],
  computedAdditionalTermsToAnnotate: documentType['decisionMetadata']['computedAdditionalTerms'],
) {

}
