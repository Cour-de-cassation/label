import { bearerTokenHandler } from './bearerTokenHandler';
import { displayModeHandler } from './displayModeHandler';

export { localStorage };

const localStorage = {
  bearerTokenHandler,
  displayModeHandler,
};
